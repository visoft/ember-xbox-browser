<div class="container">
  <div class="row">
  {{#each sortedCollection as |title|}}
    <div class="col-sm-12 col-md-6 col col-lg-4 col-xl-3">
      <div class="card">
        <div class="card-header">
          {{title.name}}
        </div>
        <div class="card-block">
          <p class="card-text"><strong>Last Unlock: </strong> {{moment-from-now title.lastUnlock}}</p>
          <p class="card-text"><strong>Gamerscore: </strong>{{title.currentGamerscore}} / {{title.maxGamerscore}}</p>
        </div>
        <div class="card-footer text-muted">
          <progress class="progress progress-success" value="{{title.currentGamerscore}}" max="{{title.maxGamerscore}}"></progress>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</div>
